# ═══════════════════════════════════════════════════════════════
#   DevShield — CI/CD Pipeline
#   Trigger: main branch ga push
#   Steps:  Build → Push (GHCR) → Deploy (Oracle Cloud VPS)
# ═══════════════════════════════════════════════════════════════

name: 🚀 DevShield CI/CD

on:
  push:
    branches: [main]

  # Manual trigger (UI dan ham ishga tushirish mumkin)
  workflow_dispatch:

# Bir vaqtda faqat bitta deploy ishlashi uchun
concurrency:
  group: deploy-production
  cancel-in-progress: false

# ─── GHCR uchun tokenlar ──────────────────────────────────────
permissions:
  contents: read
  packages: write

# ─── Environment o'zgaruvchilar ───────────────────────────────
env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/devshield


jobs:

  # ═══════════════════════════════════════════════════════════
  #   1-BOSQICH: Docker imagelarni build va GHCR ga push
  # ═══════════════════════════════════════════════════════════
  build-and-push:
    name: 🏗️ Build & Push Images
    runs-on: ubuntu-latest

    strategy:
      matrix:
        include:
          - service: django
            context: ./services/drf
            dockerfile: ./services/drf/Dockerfile
          - service: fastapi-worker
            context: ./services/fastapi
            dockerfile: ./services/fastapi/Dockerfile
          - service: bot
            context: ./services/bot
            dockerfile: ./services/bot/Dockerfile
          - service: nginx
            context: ./nginx
            dockerfile: ./nginx/Dockerfile

    steps:
      # ─── Kodni yuklab olish ──────────────────────────────
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # ─── Docker Buildx (multi-platform support) ──────────
      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ─── GHCR ga login ──────────────────────────────────
      - name: 🔑 Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ─── Docker metadata (tag va label) ──────────────────
      - name: 🏷️ Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_PREFIX }}-${{ matrix.service }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      # ─── Build va Push ───────────────────────────────────
      - name: 🐳 Build and Push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false


  # ═══════════════════════════════════════════════════════════
  #   2-BOSQICH: Oracle Cloud VPS ga deploy
  # ═══════════════════════════════════════════════════════════
  deploy:
    name: 🚀 Deploy to Production
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: production

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      # ─── docker-compose.prod.yml ni serverga yuborish ────
      - name: 📤 Copy deployment files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          source: "docker-compose.prod.yml"
          target: "~/devshield"

      # ─── SSH orqali deploy ───────────────────────────────
      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT || 22 }}
          script_stop: true
          script: |
            set -e

            echo "═══════════════════════════════════════════"
            echo "  🚀 DevShield Deploy boshlandi..."
            echo "  📅 $(date '+%Y-%m-%d %H:%M:%S')"
            echo "═══════════════════════════════════════════"

            cd ~/devshield

            # ─── GHCR ga login ─────────────────────────────
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

            # ─── Yangi imagelarni pull qilish ──────────────
            echo "📥 Yangi imagelarni yuklab olish..."
            docker compose -f docker-compose.prod.yml pull

            # ─── Eski konteynerlarni to'xtatish va yangilash
            echo "🔄 Konteynerlarni yangilash..."
            docker compose -f docker-compose.prod.yml up -d --remove-orphans

            # ─── Django migratsiyalarni ishga tushirish ────
            echo "🗄️ Migratsiyalar..."
            docker compose -f docker-compose.prod.yml exec -T django python manage.py migrate --noinput

            # ─── Static fayllar ────────────────────────────
            echo "📦 Static fayllarni yig'ish..."
            docker compose -f docker-compose.prod.yml exec -T django python manage.py collectstatic --noinput

            # ─── Eski imagelarni tozalash ──────────────────
            echo "🧹 Eski imagelarni tozalash..."
            docker image prune -f

            # ─── Health check ──────────────────────────────
            echo "🏥 Health check..."
            sleep 10

            if docker compose -f docker-compose.prod.yml ps | grep -q "Up"; then
              echo "✅ Barcha konteynerlar ishlayapti!"
              docker compose -f docker-compose.prod.yml ps
            else
              echo "❌ Ba'zi konteynerlar ishga tushmadi!"
              docker compose -f docker-compose.prod.yml ps
              docker compose -f docker-compose.prod.yml logs --tail=50
              exit 1
            fi

            echo "═══════════════════════════════════════════"
            echo "  ✅ Deploy muvaffaqiyatli yakunlandi!"
            echo "═══════════════════════════════════════════"
